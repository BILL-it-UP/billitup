node_modules/
dist/
build/
.env
.env.local
*.log
.DS_Store
server/data/*.sqlite
server/data/*.sqlite-journal
.vite/
coverage/

# Files delivered by Claude during development sessions — not part of the
# project itself, kept out so a stray `git add -A` can never sweep them in.
Claude outputs/
